<Window x:Class="Test5.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test5"
        xmlns:mf="https://schemas.mfutility.dev/wpf"
        xmlns:managers="clr-namespace:MFUtility.Mvvm.Wpf.Managers;assembly=MFUtility.Mvvm.Wpf.Framework"
        xmlns:viewModels="clr-namespace:Test5.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
        Title="MainWindow" Height="350" Width="525">
    <mf:Grid>
        <StackPanel>
            <TextBlock Text="{Binding DisplayName}"></TextBlock>
            <Button Content="GoHome" Command="{Binding GoHomeCommand}"/>
            <Button Content="GoSettings" Command="{Binding GoSettingsCommand}"/>
            <Button Content="GoBack" Command="{Binding GoBackCommand}"/>
        </StackPanel>
        <ContentControl managers:RegionManager.RegionName="Main" />
    </mf:Grid>
</Window>
