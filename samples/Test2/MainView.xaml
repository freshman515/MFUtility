<Window x:Class="Test2.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test2"
        xmlns:mf="https://schemas.mfutility.dev/wpf"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d"
        xmlns:cv="https://schemas.mfutility.converters.dev/wpf"
        xmlns:vm="clr-namespace:Test2.ViewModels"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        xmlns:managers="clr-namespace:MFUtility.Mvvm.Wpf.Managers;assembly=MFUtility.Mvvm.Wpf.Toolkit"
        Title="MainWindow" Height="450" Width="800">
    <mf:Grid Columns="* *" ColumnSpacing="10">
        <mf:StackPanel MarginTop="10">
            <Button
                Background="{Binding  Hello,Converter={x:Static cv:BoolToBrushConverter.Instance},ConverterParameter='#ff0000,#00ff00'}"
                Command="{Binding Test1Command}" Content="打log" Height="30" />
            <Button Content="Home" Command="{Binding  NavigateHomeCommand}" />
            <Button Content="Settings" Command="{Binding NavigateSettingsCommand}" />
            <Button Content="GoBack" Command="{Binding GoBackCommand}"/>
        </mf:StackPanel>
        <ContentControl managers:RegionManager.RegionName="Main" />
        <ListBox ItemsSource="{Binding Source={x:Static Member=local:GlobalParameters.Instance},Path=Logs}"></ListBox>
    </mf:Grid>
</Window>