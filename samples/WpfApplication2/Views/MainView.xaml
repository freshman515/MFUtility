<Window x:Class="WpfApplication2.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
        xmlns:mf="https://schemas.mfutility.dev/wpf"
        xmlns:viewModels="clr-namespace:WpfApplication2.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
        xmlns:managers="clr-namespace:MFUtility.Mvvm.Wpf.Framework.Managers;assembly=MFUtility.Mvvm.Wpf.Framework"
        Title="MainWindow" Height="350" Width="525">
    <mf:Grid>
        <mf:StackPanel>
            <Button Content="NavigateHome" Command="{Binding NavigateCommand}"
                    CommandParameter="Home"/>
            <Button Content="NavigateSettings" Command="{Binding NavigateCommand}"
                    CommandParameter="Settings"/>
            <Button Content="GoBack" Command="{Binding GoBackCommand}" />
        </mf:StackPanel>
        <ContentControl managers:RegionManager.RegionName="Main" />
        <ContentControl managers:RegionManager.RegionName="Main2" />
    </mf:Grid>
</Window>